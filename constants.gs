// CONSTANTS
// For use in generating tables
// google script seems to have some differences and issues with working with consts

var startDate = new Date(2017, 7, 28); // this is a Monday. queries are >= or < than next.

var excludedRSMs = ['Kelly Kenworthy'];

var excludedProperties = [];